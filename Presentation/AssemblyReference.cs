using Presentation;

namespace Presentation
{
    /// <summary>
    /// Used only to provide a reference to this assembly for scanning (e.g., AutoMapper, MediatR, etc.).
    /// </summary>
    public static class AssemblyReference
    {
        public static string Marker => nameof(AssemblyReference);
    }
}
